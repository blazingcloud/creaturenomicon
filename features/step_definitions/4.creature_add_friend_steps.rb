When /^the user types in "(.*?)" in the friend field$/ do |arg1|
  pending ("add friend using acts_as_follower gem and use api to show firends")
end

When /^the user gets autocomplete "(.*?)"$/ do |arg1|
  pending ("use jquery autocomplete")

end

Then /^you can see "(.*?)" is part of creature friends$/ do |arg1|
  pending ("do show page with list of creature's friends")
end

Then /^you go to Biebers page, and you see Tyra as a friend too$/ do
  pending ("do show follower and followed")
end